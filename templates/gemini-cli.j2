{# Gemini CLI Template #}
{# Extends base template - no special modifications needed #}
{%- extends "base.j2" -%}

{%- block content -%}
{{ super() }}

## Gemini CLI Specific Notes

### Skill Activation
Gemini CLI supports project and user discovery locations:
1. Project: `.gemini/skills/` and `.agents/skills/`
2. User: `~/.gemini/skills/`

When activated, the skill's directory is added to the agent's allowed file paths.

### Verification
Run `gemini skills list | grep llmveritas` to verify installation.
{%- endblock -%}
